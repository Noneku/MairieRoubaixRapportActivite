{% block field_rapportActiviteFile %}
    {% if entity.instance.rapportActiviteFileName %}
        <a href="{{ asset('./public/' ~ entity.instance.rapportActiviteFileName) }}" download>
            <button>Télécharger</button>
        </a>
    {% else %}
        <span>Aucun fichier</span>
    {% endif %}
{% endblock %}
